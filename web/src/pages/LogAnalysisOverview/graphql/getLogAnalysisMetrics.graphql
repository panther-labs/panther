query GetLogAnalysisMetrics($input: LogAnalysisMetricsInput) {
  getLogAnalysisMetrics(input: $input) {
    metricsResults {
      metricsName
      seriesData {
        label
        timestamps
        values
      }
    }
    intervalHours
  }
}
