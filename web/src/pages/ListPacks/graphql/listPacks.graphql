query ListPacks($input: ListPacksInput!) {
    listPacks(input: $input) {
        packs {
            ...PackFull
        }
        paging {
            totalPages
            thisPage
            totalItems
        }
    }
}