query GetLogAnalysisMetrics($input: LogAnalysisMetricsInput!) {
  getLogAnalysisMetrics(input: $input) {
    alertsBySeverity {
      series {
        label
        values
      }
      timestamps
    }
    totalAlertsDelta {
      label
      value
    }
    alertsByRuleID {
      label
      value
    }
    intervalMinutes
  }
}
