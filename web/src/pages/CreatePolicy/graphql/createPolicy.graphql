mutation CreatePolicy($input: CreateOrModifyPolicyInput!) {
  addPolicy(input: $input) {
    ...PolicyDetailsFragment
    body
    tests {
      expectedResult
      name
      resource
      resourceType
    }
  }
}
