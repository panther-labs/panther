query ListAlerts($input: ListAlertsInput) {
  alerts(input: $input) {
    alertSummaries {
      title
      creationTime
      severity
      alertId
      eventsMatched
    }
    lastEvaluatedKey
  }
}
