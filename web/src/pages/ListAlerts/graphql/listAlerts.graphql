query ListAlerts($input: ListAlertsInput) {
  alerts(input: $input) {
    alertSummaries {
      title
      creationTime
      updateTime
      severity
      alertId
      eventsMatched
    }
    lastEvaluatedKey
  }
}
