query PolicyDetails($input: GetPolicyInput!) {
  policy(input: $input) {
    ...PolicyDetailsFragment
    body
    tests {
      expectedResult
      name
      resource
      resourceType
    }
  }
}
